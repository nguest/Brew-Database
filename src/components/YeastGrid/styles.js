import { u } from '../../styles/spacing';
import { colors } from '../../styles/colors';

export const cardGrid = {
    display: 'grid',
    gridTemplateColumns: '1fr 1fr 1fr',//repeat(auto-fill, minmax(376px, 1fr))',
    gridGap: u * 5,
};

